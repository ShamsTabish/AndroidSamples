<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acadgild.session10">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MaterialDesign" />
        <activity android:name=".CardViewDemo" />
        <activity android:name=".FloatingActionButtonDemo" />
        <activity android:name=".PaletteDemo"></activity>
        <activity
            android:name=".NavigationDrawerDemo"
            android:label="@string/title_activity_navigation_drawer_demo"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".SwipeRefreshDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>